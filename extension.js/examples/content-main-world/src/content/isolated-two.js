// ISOLATED world content script (group 1).
// Second isolated entry to push the MAIN world group to index 2,
// ensuring the bridge canonical index (3) diverges from its array position (2).
console.log('[content-main-world] isolated-two loaded (ISOLATED world)')
